interface:
  display_name: "Re-analyze Memory System"
  short_description: "Re-read a system the atlas already covers, at a newer commit"
  default_prompt: "Use $reanalyze-memory-system to re-read a repository Agent Memory Atlas already has a report for."
