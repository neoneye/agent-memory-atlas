interface:
  display_name: "Use the Atlas"
  short_description: "Design memory for another product from the atlas, brief first"
  default_prompt: "Use $use-the-atlas in decide mode to work out which memory patterns this repository actually needs. Do not modify the repository."
