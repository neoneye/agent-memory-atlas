{
  "name": "agent-memory-atlas",
  "version": "1.0.0",
  "private": true,
  "description": "A code-grounded field guide to agent memory architectures.",
  "scripts": {
    "build": "bash scripts/build_site.sh",
    "serve": "python3 -m http.server 4173 --directory docs",
    "test": "bash scripts/test_site.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
