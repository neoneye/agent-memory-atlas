# Repositories the atlas examined and deliberately did not write up.
#
# Everything under content/systems/ is imported automatically from report
# frontmatter, so nothing that has a report belongs here. This file is for the
# other outcome: a repository that was read and set aside. Those decisions live
# in content/overview.md as prose — a sentence explaining what was found and why
# it is out of scope — and a sentence is not something a program should parse
# when the consequence is spending a slot of the maintainer's daily analysis
# budget. So the machine-readable half is kept here, by hand.
#
# One `owner/repo` per line, with the reason after a `#`.
#
# Format:
#   some-owner/some-repo   # examined 2026-08-04: a task queue, not memory
freshhillyer/xiaoo   # examined 2026-09-11 at 3acadbb3: long-term memory is an unpublished MCP server (RAM-A); the in-tree memory crate has no caller
abhishekax7/hydra   # examined 2026-09-11 at 9b33d894: project bookkeeping and workflow checkpoints; no stored finding is recalled into a later run
n8group-oss/omarchy-session-memory   # examined 2026-09-11 at fe4f518a: tmux and agent-session restoration; pointers to transcripts, nothing recalled
